{
  "name": "kayo-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "build": "cp -r . dist/ && rm -rf dist/dist"
  },
  "dependencies": {
    "firebase": "^8.10.0"
  }
}